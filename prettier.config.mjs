// Prettier config for @iobroker/eslint-config integration.
// Mirrors the previous .prettierrc.json so formatting stays unchanged.
export default {
    singleQuote: true,
    tabWidth: 4,
    printWidth: 120,
    trailingComma: 'all',
    semi: true,
    endOfLine: 'lf',
};
