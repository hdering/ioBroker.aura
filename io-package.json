{
  "common": {
    "name": "aura",
    "version": "0.51.1",
    "readme": "https://hdering.github.io/ioBroker.aura/",
    "titleLang": {
      "en": "Aura",
      "de": "Aura",
      "ru": "Aura",
      "pt": "Aura",
      "nl": "Aura",
      "fr": "Aura",
      "it": "Aura",
      "es": "Aura",
      "pl": "Aura",
      "uk": "Aura",
      "zh-cn": "Aura"
    },
    "desc": {
      "en": "Modern visualization dashboard for ioBroker",
      "de": "Modernes Visualisierungs-Dashboard für ioBroker",
      "ru": "Современная панель визуализации для ioBroker",
      "pt": "Painel de visualização moderno para ioBroker",
      "nl": "Modern visualisatiedashboard voor ioBroker",
      "fr": "Tableau de bord de visualisation moderne pour ioBroker",
      "it": "Dashboard di visualizzazione moderna per ioBroker",
      "es": "Panel de visualización moderno para ioBroker",
      "pl": "Nowoczesny panel wizualizacji dla ioBroker",
      "uk": "Сучасна панель візуалізації для ioBroker",
      "zh-cn": "ioBroker 的现代可视化仪表板"
    },
    "authors": [
      "Hermann Dering <aura@dering-online.de>"
    ],
    "keywords": [
      "visualization",
      "dashboard",
      "vis",
      "tablet",
      "ical",
      "calendar",
      "evcc",
      "gauge",
      "chart"
    ],
    "licenseInformation": {
      "license": "MIT",
      "type": "free"
    },
    "icon": "aura.png",
    "extIcon": "https://raw.githubusercontent.com/hdering/ioBroker.aura/main/admin/aura.png",
    "messagebox": true,
    "protectedNative": [
      "mcpToken",
      "mcpClientConfig"
    ],
    "logTransporter": true,
    "type": "visualization",
    "mode": "daemon",
    "compact": false,
    "tier": 3,
    "connectionType": "local",
    "dataSource": "push",
    "noIntro": true,
    "platform": "Javascript/Node.js",
    "adminUI": {
      "config": "json"
    },
    "localLinks": {
      "frontend": {
        "link": "%protocol%://%ip%:8095/",
        "name": {
          "en": "Aura Frontend",
          "de": "Aura Frontend",
          "ru": "Aura Frontend",
          "pt": "Aura Frontend",
          "nl": "Aura Frontend",
          "fr": "Aura Frontend",
          "it": "Aura Frontend",
          "es": "Aura Frontend",
          "pl": "Aura Frontend",
          "uk": "Aura Frontend",
          "zh-cn": "Aura Frontend"
        }
      },
      "backend": {
        "link": "%protocol%://%ip%:8095/#/admin",
        "name": {
          "en": "Aura Backend",
          "de": "Aura Backend",
          "ru": "Aura Backend",
          "pt": "Aura Backend",
          "nl": "Aura Backend",
          "fr": "Aura Backend",
          "it": "Aura Backend",
          "es": "Aura Backend",
          "pl": "Aura Backend",
          "uk": "Aura Backend",
          "zh-cn": "Aura Backend"
        }
      }
    },
    "dependencies": [
      {
        "js-controller": ">=6.0.11"
      }
    ],
    "globalDependencies": [
      {
        "admin": ">=7.8.23"
      }
    ],
    "news": {
      "0.51.1": {
        "en": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "de": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "ru": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "pt": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "nl": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "fr": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "it": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "es": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "pl": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "uk": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)",
        "zh-cn": "Advanced chart - a consumption (\"delta\") chart no longer runs on past its own data: the time axis used to end half a bucket after the newest reading, leaving an empty strip on the right, and now ends where the data does - so the curve reaches the right edge just as the bars reach the left one (#598)\n🌟 New feature: Thermostat - new \"Rundskala\" layout: a 270 dial with a draggable handle, the setpoint in its centre and the +/- buttons in the arc gap; the scale colour is configurable, either fixed or from a colour-threshold scale (#599)"
      },
      "0.51.0": {
        "en": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "de": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "ru": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "pt": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "nl": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "fr": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "it": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "es": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "pl": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "uk": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach",
        "zh-cn": "🌟 New feature: Distribution chart, fill level and gauge - the scale can now come from datapoints instead of fixed numbers: a group takes its 100 % from a datapoint (a prepayment, a budget), the unused part becomes a \"Rest\" segment and the bar stack direction can be flipped, so the used part sits at the bottom; the gauge min/max datapoints are now offered in the editor at all (#596)\nAdvanced chart - a consumption (\"delta\") bar series no longer pushes the time axis out past the selected period: the window now opens on the same day/hour boundary the bars sit on, so lines and bars start at the same point instead of the line appearing to begin half a day late (#598)\nAdvanced chart - decimal places and thousands separator moved from the options panel into the \"Manage datapoints\" dialog, whose tabs now run Mode (with a tip on what each mode is for), Number format, Series, Values - and a single series can override decimals and separator for itself (#600)\nList / Dynamic list - the row displays caught up with their standalone widgets: the slider brings scale and step, colour, bar look, track size, value / unit / min-max labels, write-on-release and a read-only progress bar; the input field a number range and a multi-line text area; the buttons colour, icon, image or HTML per button, a JSON datapoint as their source and a dropdown for long lists; the shutter a position slider in the row, a feedback datapoint, inverted counting and the slat control; on top of that a value mapping can draw an image per state and compare with an operator, and a window/door contact can show a lock datapoint as a padlock\nList / Dynamic list - the per-datapoint editor now runs Datapoint, Label, Display, Second line, Conditions, Colour thresholds, Behaviour, so the display and its settings sit right below the name\nList / Dynamic list - two display fixes: the dynamic list's \"Slider\" and \"Value\" displays now actually render (a slider used to be drawn only when the datapoint name looked like a dimmer), and a switch entry in the card layout now fills its cell with the labelled button instead of keeping the compact toggle\nDynamic list - the display of the datapoints (switch, slider, value mapping ...) can now be set once for the whole list in the datapoint dialog, including that display's own settings, while a single datapoint can still override it; decimals, thousands separator and the colour scale can now also be set per row instead of only list-wide\n🌟 New feature: Conditions - a rule can now set the text size as well: per element (title / value) in the widget conditions, and in the row rules of both lists, their second line and the custom-layout cells; the field sits above the text colour and empty keeps the configured size\nSlider - the track thickness set in the editor is now applied (the field was written but never read)\nEditor - dialogs no longer open partly off screen after a switch to a smaller resolution: the remembered size is capped to the current window (and kept for the bigger screen), and a dialog can no longer be dragged out of reach"
      },
      "0.50.10": {
        "en": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "de": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "ru": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "pt": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "nl": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "fr": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "it": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "es": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "pl": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "uk": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to",
        "zh-cn": "🌟 New feature: Advanced chart - a timeseries chart can now mix history series with JSON datapoint series on one time axis, e.g. measured values plus a solar forecast (#595)\n🌟 New feature: Advanced chart - mode and series moved into the \"Manage datapoints\" dialog: series list on the left, the selected series in full detail on the right, global settings stay in the options panel\n🌟 New feature: Advanced chart - switching the mode no longer overwrites the series: a chart with a JSON series kept its data source after a look into another mode and back\nAdvanced chart - the value-label default and the stack percentage moved into the \"Manage datapoints\" dialog (tab \"Values\"), next to the series they apply to"
      },
      "0.50.9": {
        "en": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "de": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "ru": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "pt": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "nl": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "fr": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "it": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "es": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "pl": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "uk": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)",
        "zh-cn": "Advanced chart - leaving the day navigation for a rolling range (7/30 days) no longer keeps the chart framed on that single day (#594)"
      },
      "0.50.8": {
        "en": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "de": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "ru": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "pt": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "nl": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "fr": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "it": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "es": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "pl": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "uk": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied",
        "zh-cn": "🌟 New feature: Dynamic list - one icon for all rows (icon, size and colour), set in the new \"Icon\" tab of the datapoint dialog; a per-datapoint icon and conditions still override it\nColor picker - dragging a colour no longer freezes the UI: the value now reaches the config at most every 120 ms, with the final one always applied"
      },
      "0.50.7": {
        "en": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "de": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "ru": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "pt": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "nl": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "fr": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "it": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "es": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "pl": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "uk": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)",
        "zh-cn": "🌟 New feature: Image - datapoints holding raw SVG markup are now displayed, e.g. the guest WLAN QR code of fb-checkpresence (#592)\nImage - optional background colour behind the picture, keeps transparent SVGs such as QR codes readable on dark themes (#592)\n🌟 New feature: Mediaplayer - device detection now recognises any adapter that follows the ioBroker media roles (yamaha, denon, volumio, ...), including its volume range, mute and input (#593)\nMediaplayer - play/pause button now reads playback states that are a numbered enum, e.g. a Yamaha receiver reporting 0 = Play (#593)\nStatic and dynamic list - the Switch display now offers the same options as the Switch widget: own write values per state (e.g. 0/255, ON/OFF), a separate status datapoint for devices that split command and feedback, condition-based on/off evaluation and an icon or image instead of the slide toggle (#591)\n🌟 New feature: Dynamic list - the Switch display now also works for string and enum datapoints and gained the switch style, on/off icons, icon size and confirmation prompt the static list already had (#591)\nCharts, lists and value display - new \"show as negative\" option in the value conversion, for figures that are logged as positive but belong below the zero line, such as grid feed-in or battery charging (#594)\nAdvanced chart - consumption bars (delta aggregation) came out as a row of zeros when a negative display factor was set; the counter is now differenced before the sign is applied (#594)\nAdvanced chart - the day navigation gained a date field, so a day can be picked directly instead of stepping there one day at a time (#594)\nAdvanced chart - a bar axis now always includes zero, so bar lengths stay proportional to their values and a series drawn downwards keeps its zero line; pure line charts still fit their own range, and an explicit axis minimum still wins (#594)\nAdvanced chart - horizontal grid lines were missing when every series was assigned to the right y axis (#594)"
      },
      "0.50.6": {
        "en": "Release v0.50.6",
        "de": "Release v0.50.6",
        "ru": "Release v0.50.6",
        "pt": "Release v0.50.6",
        "nl": "Release v0.50.6",
        "fr": "Release v0.50.6",
        "it": "Release v0.50.6",
        "es": "Release v0.50.6",
        "pl": "Release v0.50.6",
        "uk": "Release v0.50.6",
        "zh-cn": "Release v0.50.6"
      }
    }
  },
  "native": {
    "customUrl": "",
    "port": 8095,
    "socketPort": 8082,
    "socketSecure": false,
    "socketUrl": "",
    "secure": false,
    "certPublic": "",
    "certPrivate": "",
    "certChained": "",
    "fsRoots": [
      {
        "label": "ioBroker Files",
        "path": "/opt/iobroker/iobroker-data/files"
      }
    ],
    "fsAllowWrite": false,
    "timerTickSeconds": 30,
    "perfTracking": true,
    "perfWidgetTracking": false,
    "messageHistorySize": 100,
    "messageRetentionDays": 30,
    "mcpEnabled": false,
    "mcpToken": "",
    "mcpMode": "read",
    "mcpClientConfig": ""
  },
  "objects": [],
  "instanceObjects": [
    {
      "_id": "info",
      "type": "channel",
      "common": {
        "name": "Information"
      },
      "native": {}
    },
    {
      "_id": "info.connection",
      "type": "state",
      "common": {
        "name": "If adapter connected to ioBroker",
        "type": "boolean",
        "role": "indicator.connected",
        "read": true,
        "write": false,
        "def": false
      },
      "native": {}
    },
    {
      "_id": "info.activeLayout",
      "type": "state",
      "common": {
        "name": "Currently displayed layout",
        "type": "string",
        "role": "text",
        "read": true,
        "write": false,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "info.activeSection",
      "type": "state",
      "common": {
        "name": "Currently displayed section (Bereich)",
        "type": "string",
        "role": "text",
        "read": true,
        "write": false,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "info.activeTab",
      "type": "state",
      "common": {
        "name": "Currently displayed tab",
        "type": "string",
        "role": "text",
        "read": true,
        "write": false,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "info.version",
      "type": "state",
      "common": {
        "name": "Installed adapter version",
        "type": "string",
        "role": "info.version",
        "read": true,
        "write": false,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "config",
      "type": "channel",
      "common": {
        "name": "Configuration"
      },
      "native": {}
    },
    {
      "_id": "config.dashboard",
      "type": "state",
      "common": {
        "name": "Dashboard configuration",
        "type": "string",
        "role": "json",
        "read": true,
        "write": true,
        "def": "{\"widgets\":[]}"
      },
      "native": {}
    },
    {
      "_id": "config.themeMode",
      "type": "channel",
      "common": {
        "name": "Theme mode overrides (frontend & admin independently)"
      },
      "native": {}
    },
    {
      "_id": "config.themeMode.frontend",
      "type": "state",
      "common": {
        "name": "Frontend theme mode ('dark'|'light'|''); empty = no override",
        "type": "string",
        "role": "text",
        "read": true,
        "write": true,
        "def": "",
        "states": {
          "dark": "dark",
          "light": "light"
        }
      },
      "native": {}
    },
    {
      "_id": "config.themeMode.adminUi",
      "type": "state",
      "common": {
        "name": "Admin theme mode ('dark'|'light'|''); empty = no override",
        "type": "string",
        "role": "text",
        "read": true,
        "write": true,
        "def": "",
        "states": {
          "dark": "dark",
          "light": "light"
        }
      },
      "native": {}
    },
    {
      "_id": "navigate",
      "type": "channel",
      "common": {
        "name": "Navigation"
      },
      "native": {}
    },
    {
      "_id": "navigate.url",
      "type": "state",
      "common": {
        "name": "Navigate to URL or tab slug",
        "type": "string",
        "role": "url",
        "read": true,
        "write": true,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "popup",
      "type": "channel",
      "common": {
        "name": "Popups"
      },
      "native": {}
    },
    {
      "_id": "popup.open",
      "type": "state",
      "common": {
        "name": "Open a popup view (name, id or JSON {view,dp,title})",
        "type": "string",
        "role": "text",
        "read": true,
        "write": true,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "admin",
      "type": "channel",
      "common": {
        "name": "Admin access"
      },
      "native": {}
    },
    {
      "_id": "timers",
      "type": "channel",
      "common": {
        "name": "Zeitschaltuhren (timer widgets)"
      },
      "native": {}
    },
    {
      "_id": "admin.pinHash",
      "type": "state",
      "common": {
        "name": "Admin PIN hash (FNV-1a, managed by frontend)",
        "type": "string",
        "role": "text",
        "read": true,
        "write": true,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "calendar",
      "type": "channel",
      "common": {
        "name": "Calendar fetch relay"
      },
      "native": {}
    },
    {
      "_id": "calendar.request",
      "type": "state",
      "common": {
        "name": "Calendar fetch request (JSON: {id, url})",
        "type": "string",
        "role": "json",
        "read": true,
        "write": true,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "calendar.response",
      "type": "state",
      "common": {
        "name": "Calendar fetch response (JSON: {id, content|error})",
        "type": "string",
        "role": "json",
        "read": true,
        "write": true,
        "def": ""
      },
      "native": {}
    },
    {
      "_id": "logs",
      "type": "channel",
      "common": {
        "name": "Adapter log stream (for AdapterLogsWidget)"
      },
      "native": {}
    },
    {
      "_id": "logs.latest",
      "type": "state",
      "common": {
        "name": "Latest log entry (JSON, updated on every system log)",
        "type": "string",
        "role": "json",
        "read": true,
        "write": false,
        "def": ""
      },
      "native": {}
    }
  ]
}
